<Window
    x:Class="ClipPocketWin.EditTextWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edit Text">

    <Grid Background="Transparent" Padding="10">
        <Border
            Background="#14000000"
            CornerRadius="10"
            Padding="8">
            <TextBox
                x:Name="EditorTextBox"
                AcceptsReturn="True"
                Background="Transparent"
                BorderBrush="Transparent"
                BorderThickness="0"
                FontFamily="Consolas"
                FontSize="14"
                Foreground="#F2FFFFFF"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsSpellCheckEnabled="False"
                KeyDown="EditorTextBox_KeyDown"
                PlaceholderText="Edit clipboard text..."
                SelectionHighlightColor="#FF58A6FF"
                TextWrapping="Wrap"
                VerticalAlignment="Stretch"
                VerticalContentAlignment="Stretch">
                <TextBox.Resources>
                    <SolidColorBrush x:Key="TextControlBorderBrush" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlBorderBrushPointerOver" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlBorderBrushFocused" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent" />
                    <SolidColorBrush x:Key="TextControlSelectionHighlightColorThemeBrush" Color="#FF58A6FF" />
                </TextBox.Resources>
            </TextBox>
        </Border>
    </Grid>
</Window>
